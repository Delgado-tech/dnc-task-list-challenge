import './style.scss';

export default function Header() {
    return (
        <header className="header">
            <figure>
                <img src="/logo.svg" alt="logo" />
            </figure>
        </header>
    );
}